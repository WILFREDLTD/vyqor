// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["official_company_schema"]
}

model Contact {
  id         Int      @id @default(autoincrement())
  firstName  String
  lastName   String
  email      String
  phone      String
  service    String
  message    String
  createdAt  DateTime @default(now())

  @@schema("official_company_schema")
}

model Consultation {
  id                Int      @id @default(autoincrement())
  firstName         String
  lastName          String
  email             String
  phone             String
  service           String
  selectedDate      DateTime
  selectedTime      String   // Format: "09:00" (24-hour)
  additionalNotes   String?
  googleMeetLink    String
  createdAt         DateTime @default(now())

  @@schema("official_company_schema")
}